# Secrets - never commit
ssh/
*.pem
*.key
.credentials*
.token*

# Google Drive credentials
gdrive/

# Binaries (large files, downloaded at install time)
bin/rclone

# rclone config (contains tokens)
rclone/

# OS
.DS_Store
Thumbs.db
